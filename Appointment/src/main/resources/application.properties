spring.application.name=Appointment

server.port=9200
spring.datasource.url=jdbc:mysql://localhost:3306/schedule?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
#?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

eureka.instance.instance-id=${spring.application.name}:${random.value}

APPOINTMENT_NOT_FOUND= Appointment not found
APPOINTMENT_ALREADY_CANCELLED= Appointment already canceled

DOCTOR_NOT_FOUND= Doctor not found
PATIENT_NOT_FOUND= Patient not found

